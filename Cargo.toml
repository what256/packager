[workspace]
members = ["crates/packager-core", "crates/packager-cli", "src-tauri"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/what256/packager"

[profile.release]
lto = true
codegen-units = 1
strip = true
